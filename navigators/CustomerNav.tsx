import { View, Text } from 'react-native';
import React from 'react';

const CustomerNav = ({ Stack }: any) => {
  return (
    <View>
	 <Text></Text>
    </View>
  );
};

export default CustomerNav;
